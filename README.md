# Terraform Provider desec.io

This is a terraform provider for [desec.io](https://desec.io) API.

## Build provider

Run the following command to build the provider

```shell
$ go build -o terraform-provider-desec
```
